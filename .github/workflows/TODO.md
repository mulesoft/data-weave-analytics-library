# TODOs for releasing to global exchange

- Add global `qax`, `prod` and `prod-eu` credentials as secrets
- Change orgids
- Change pom.xml profile urls
- Add all 3 releases to the release_workflow.yml